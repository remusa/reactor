<h1>New Comment</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.comment_path(@conn, :create)) %>

<span><%= link "Back", to: Routes.podcast_path(@conn, :show, @comment.podcast_id) %></span>
